{# templates/ressources/new.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Créer une Ressource{% endblock %}

{% block body %}
<div class="container mx-auto p-6">
    <h1 class="text-2xl font-semibold text-gray-800 mb-4">Créer une Nouvelle Ressource</h1>

    {{ form_start(form) }}
    <div class="mb-4">
        {{ form_row(form.intitule, {'attr': {'class': 'border rounded-md p-2 w-full'}}) }}
    </div>
    <div class="mb-4">
        {{ form_row(form.presentation, {'attr': {'class': 'border rounded-md p-2 w-full'}}) }}
    </div>
    <div class="mb-4">
        {{ form_row(form.url_du_document_physique, {'attr': {'class': 'border rounded-md p-2 w-full'}}) }}
    </div>
    <div class="mb-4">
        {{ form_row(form.support, {'attr': {'class': 'border rounded-md p-2 w-full'}}) }}
    </div>
    <div class="mb-4">
        {{ form_row(form.Nature, {'attr': {'class': 'border rounded-md p-2 w-full'}}) }}
    </div>

    <div class="mb-4">
        <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600">
            Créer la Ressource
        </button>
    </div>
    {{ form_end(form) }}
</div>
{% endblock %}
